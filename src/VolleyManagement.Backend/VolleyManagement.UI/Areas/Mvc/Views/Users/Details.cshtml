@model User

@using VolleyManagement.Domain.UsersAggregate;

@{
    ViewBag.Title = "User details";//Resources.UI.UserViews.UserDetailsTitle;
}

<h2>@ViewBag.Title</h2>

<fieldset>
    <legend>@Resources.UI.UserViews.UserLegend</legend>

    <div class="display-label">@Html.LabelFor(model => model.UserName)</div>
    <div class="display-field">@Model.UserName</div>

    <div class="display-label">@Html.LabelFor(model => model.Email)</div>
    <div class="display-field">@Html.DisplayFor(model => model.Email)</div>

    <div class="display-label">@Html.LabelFor(model => model.FullName)</div>
    <div class="display-field">@Model.FullName</div>

    <div class="display-label">@Html.LabelFor(model => model.PhoneNumber)</div>
    <div class="display-field">@Html.DisplayFor(model => model.PhoneNumber)</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.Id }) |
    @Html.ActionLink(Resources.UI.UserViews.BackToList, "Index")
</p>


